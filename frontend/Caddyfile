{
	auto_https off
}

:{$PORT} {
	root * /srv
	encode gzip

	handle /health {
		respond "ok" 200
	}

	route {
		try_files {path} /index.html
		@html path /index.html
		header @html Cache-Control "no-cache, must-revalidate"
		file_server
	}
}
